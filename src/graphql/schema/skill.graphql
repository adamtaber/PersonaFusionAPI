type Skill {
  skillId: Int!,
  name: String!,
  type: String!,
  effect: String!,
  cost: Int
}

type Query {
  allSkills: [Skill]!
  skillById(
    skillId: Int!
  ): Skill
  skillByName(
    name: String!
  ): [Skill]!
}